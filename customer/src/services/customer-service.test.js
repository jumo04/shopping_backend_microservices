// which service it is
//it necessary that the name of the service is equal to the test
describe('CustomerService', () => {
    // which function it is
    describe('SignIn', () => {
        // which scenario we are testing
        test("validate user inputs", () => {});

        test("validate response", () => {});
    })
})